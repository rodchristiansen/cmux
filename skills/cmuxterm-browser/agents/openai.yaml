interface:
  display_name: "cmuxterm Browser"
  short_description: "Automate cmuxterm webview surfaces with snapshot/ref workflows."
  default_prompt: "Use this skill for browser automation via cmuxterm CLI: identify target surface, snapshot interactive refs, perform actions, wait for state changes, and re-snapshot to avoid stale refs."
